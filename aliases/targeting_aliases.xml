<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE MudletPackage>
<MudletPackage version="1.001">
	<AliasPackage>
		<AliasGroup isActive="yes" isFolder="yes">
			<name>Targeting</name>
			<script></script>
			<command></command>
			<packageName></packageName>
			<regex></regex>
			<AliasGroup isActive="yes" isFolder="yes">
				<name>First Target</name>
				<script>--syntax: tgt OR tgt xxx

targeting = targeting or {}

if matches[3] then
  targeting.first = matches[4]
  if matches[4] == "dark" then
    cecho("First target is set as: &lt;red&gt;" .. matches[4] .. "&lt;white&gt;.\n")
  elseif matches[4] == "seanchan" then
    cecho("First target is set as: &lt;magenta&gt;" .. matches[4] .. "&lt;white&gt;.\n")
  elseif matches[4] == "human" then
    cecho("First target is set as: &lt;cyan&gt;" .. matches[4] .. "&lt;white&gt;.\n")
  else
    cecho("First target is set as: &lt;gold&gt;" .. matches[4] .. "&lt;white&gt;.\n")
  end
elseif targeting.first then
  if targeting.first == "dark" then
    cecho("First target is set as: &lt;red&gt;dark&lt;white&gt;.\n")
  elseif targeting.first == "seanchan" then
    cecho("First target is set as: &lt;magenta&gt;seanchan&lt;white&gt;.\n")
  elseif targeting.first == "human" then
    cecho("First target is set as: &lt;cyan&gt;human&lt;white&gt;.\n")
  else
    cecho("First target is set as: &lt;gold&gt;" .. targeting.first .. "&lt;white&gt;.\n")
  end
else
  cecho("&lt;red&gt;Target not set!\n")
end</script>
				<command></command>
				<packageName></packageName>
				<regex>(^tgt$)|(^tgt (\S+)$)</regex>
				<Alias isActive="yes" isFolder="no">
					<name>Attack Target</name>
					<script>--syntax: p

targeting=targeting or {}

local target=targeting.first

if target then
	send("k "..target)
else
	cecho("&lt;red&gt;Target not set!\n")
end</script>
					<command></command>
					<packageName></packageName>
					<regex>^p$</regex>
				</Alias>
				<Alias isActive="yes" isFolder="no">
					<name>Attack h.Target</name>
					<script>--syntax: o

targeting=targeting or {}

local target=targeting.first

if target then
	send("k h."..target)
else
	cecho("&lt;red&gt;Target not set!\n")
end</script>
					<command></command>
					<packageName></packageName>
					<regex>^o$</regex>
				</Alias>
				<Alias isActive="yes" isFolder="no">
					<name>Bash Target</name>
					<script>--syntax: q

targeting=targeting or {}

local target=targeting.first

if target then
	send("bash "..target)
else
	cecho("&lt;red&gt;Target not set!\n")
end</script>
					<command></command>
					<packageName></packageName>
					<regex>^q$</regex>
				</Alias>
				<Alias isActive="yes" isFolder="no">
					<name>Backstab/Charge Target</name>
					<script>--syntax: ct OR ct xxx

targeting = targeting or {}

local target = targeting.first
local stab = targeting.backstabbing
local charge = targeting.charging

if matches[3] then
  if charge then
    send("charge h." .. matches[4])
  elseif stab then
    send("backstab h." .. matches[4])
  else
    cecho("&lt;red&gt;No pk method set\n")
  end
else
  if target then
    if charge then
      send("charge h." .. target)
    elseif stab then
      send("backstab h." .. target)
    else
      cecho("&lt;red&gt;No pk method set\n")
    end
  else
    cecho("&lt;red&gt;Target not set!\n")
  end
end</script>
					<command></command>
					<packageName></packageName>
					<regex>(^ct$)|(^ct (\S+)$)</regex>
				</Alias>
				<Alias isActive="yes" isFolder="no">
					<name>Delete Target</name>
					<script>--syntax: deltgt

targeting=targeting or {}

targeting.first=nil
cecho("&lt;red&gt;First target deleted!\n")</script>
					<command></command>
					<packageName></packageName>
					<regex>^deltgt$</regex>
				</Alias>
			</AliasGroup>
			<AliasGroup isActive="yes" isFolder="yes">
				<name>Second Target</name>
				<script>--syntax: tgts OR tgts xxx

targeting = targeting or {}

if matches[3] then
  targeting.second = matches[4]
  if matches[4] == "dark" then
    cecho("Second target is set as: &lt;red&gt;" .. matches[4] .. "&lt;white&gt;.\n")
  elseif matches[4] == "seanchan" then
    cecho("Second target is set as: &lt;magenta&gt;" .. matches[4] .. "&lt;white&gt;.\n")
  elseif matches[4] == "human" then
    cecho("Second target is set as: &lt;cyan&gt;" .. matches[4] .. "&lt;white&gt;.\n")
  else
    cecho("Second target is set as: &lt;gold&gt;" .. matches[4] .. "&lt;white&gt;.\n")
  end
elseif targeting.second then
  if targeting.second == "dark" then
    cecho("Second target is set as: &lt;red&gt;dark&lt;white&gt;.\n")
  elseif targeting.second == "seanchan" then
    cecho("Second target is set as: &lt;magenta&gt;seanchan&lt;white&gt;.\n")
  elseif targeting.second == "human" then
    cecho("Second target is set as: &lt;cyan&gt;human&lt;white&gt;.\n")
  else
    cecho("Second target is set as: &lt;gold&gt;" .. targeting.second .. "&lt;white&gt;.\n")
  end
else
  cecho("&lt;red&gt;Target not set!\n")
end</script>
				<command></command>
				<packageName></packageName>
				<regex>(^tgts$)|(^tgts (\S+)$)</regex>
				<Alias isActive="yes" isFolder="no">
					<name>Attack Target</name>
					<script>--syntax: pp

targeting=targeting or {}

local target=targeting.second

if target then
	send("k "..target)
else
	cecho("&lt;red&gt;Target not set!\n")
end
</script>
					<command></command>
					<packageName></packageName>
					<regex>^pp$</regex>
				</Alias>
				<Alias isActive="yes" isFolder="no">
					<name>Attack h.Target</name>
					<script>--syntax: oo

targeting=targeting or {}

local target=targeting.second

if target then
	send("k h."..target)
else
	cecho("&lt;red&gt;Target not set!\n")
end</script>
					<command></command>
					<packageName></packageName>
					<regex>^oo$</regex>
				</Alias>
				<Alias isActive="yes" isFolder="no">
					<name>Bash Target</name>
					<script>--syntax: qq

targeting=targeting or {}

local target=targeting.second

if target then
	send("bash "..target)
else
	cecho("&lt;red&gt;Target not set!\n")
end</script>
					<command></command>
					<packageName></packageName>
					<regex>^qq$</regex>
				</Alias>
				<Alias isActive="yes" isFolder="no">
					<name>Backstab/Charge Target</name>
					<script>--syntax: ctt OR ctt xxx

targeting = targeting or {}

local target = targeting.second
local stab = targeting.backstabbing
local charge = targeting.charging

if matches[3] then
  if charge then
    send("charge h." .. matches[4])
  elseif stab then
    send("backstab h." .. matches[4])
  else
    cecho("&lt;red&gt;No pk method set\n")
  end
else
  if target then
    if charge then
      send("charge h." .. target)
    elseif stab then
      send("backstab h." .. target)
    else
      cecho("&lt;red&gt;No pk method set\n")
    end
  else
    cecho("&lt;red&gt;Target not set!\n")
  end
end</script>
					<command></command>
					<packageName></packageName>
					<regex>(^ctt$)|(^ctt (\S+)$)</regex>
				</Alias>
				<Alias isActive="yes" isFolder="no">
					<name>Delete Target</name>
					<script>--syntax: deltgts

targeting=targeting or {}

targeting.second=nil
cecho("&lt;red&gt;Second target deleted!\n")</script>
					<command></command>
					<packageName></packageName>
					<regex>^deltgts$</regex>
				</Alias>
			</AliasGroup>
			<Alias isActive="yes" isFolder="no">
				<name>Switch PK Modes</name>
				<script>--syntax: pkmode

targeting=targeting or {}

local charge=targeting.charging
local stab=targeting.backstabbing

if charge then
	targeting.charging=false
	targeting.backstabbing=true
	cecho("Set at pk method: &lt;orange&gt;Backstabbing\n")
elseif stab then
	targeting.backstabbing=false
	cecho("&lt;red&gt;No pk method set\n")
else
	targeting.charging=true
	cecho("Set at pk method: &lt;green&gt;Charge\n")
end</script>
				<command></command>
				<packageName></packageName>
				<regex>^pkmode$</regex>
			</Alias>
		</AliasGroup>
	</AliasPackage>
</MudletPackage>
